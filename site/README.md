Current production site files live at the repository root for Cloudflare Pages deployment.

This `/site` directory was created during the audit as a future consolidation target.

Recovered live-only asset:
- `../qualify.html` was pulled from `https://finallysettled.com/qualify.html`

Notes:
- Root deploy remains the current source for `index.html`, `/apply`, `/request-showing`, `/agents`, `/how-it-works`, `/blog`, and `/homes`.
- No safe bulk move into `/site` was performed during this audit.
